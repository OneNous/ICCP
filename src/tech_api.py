"""Flask routes dedicated to the tech app (claude.md).

TODO: split from ``dashboard`` when commissioning/API surface stabilizes. The main
local HTTP surface today is ``dashboard`` (read-only ``/api/*`` plus static UI).
"""
